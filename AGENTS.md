* Maintain all js code inside the `index.js` file.
* Do not use any npm packages or build steps or anything involving nodejs, everything must work in modern browsers as is.
* In some cases, it's okay to load third party libs from cdnjs, but all such imports must be vetted by the user.
* Keep the code simple and clear. Model the new code you write after the code that is already there.
